# Invariant Liquibase settings baked into the migrate image. Connection
# details (url/username/password) and dims are supplied at run time by
# compose/kube, since they vary by environment.
changelog-file=changelog/changelog-master.yaml